# Netlify detects Next.js automatically; this file pins the build settings.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"

# Production builds are indexable and refuse to build with incomplete business identity.
[context.production.environment]
  SITE_ENV = "production"
